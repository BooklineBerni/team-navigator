{
  "name": "berni-navigator",
  "private": true,
  "version": "0.0.0",
  "description": "Berni Navigator — task & roadmap manager (static HTML/JS, no build step). Tests use Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:install": "playwright install chromium --with-deps",
    "test:smoke": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
